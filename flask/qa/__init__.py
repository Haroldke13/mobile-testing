"""Mobile-testing QA session capture.

Package marker. Originally only reporting.py and validation.py were
committed; app.py imported config, forms, storage and testcases as well, so
the application could not start. Those four were written 2026-09-20 against
the call signatures app.py already used, and against the shapes that
data/test_cases.csv and data/demo_session.json already fix.
"""
